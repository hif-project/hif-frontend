# Known-good upstream refs this repo's CI builds against.
# Bump deliberately, in a reviewed PR, when adopting an upstream fix.
#
# Moved forward to adopt hif-project/hif-core#17, which makes the subtrees
# returned by HIFSemantics::getSimplifiedSymbol() closed under destination
# semantics. Without it the clog2_replication test added alongside this bump
# aborts in mapStandardSymbols with "Expected declaration" (#15). Full
# hif-frontend suite verified against this ref: 9/9.
HIF_CORE_REF=0fc2751d1b4a2fc30fbedb14639e3ede7a6b7433
